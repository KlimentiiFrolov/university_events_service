# notification_client

Клиент, который подключается к `notification_service` по WebSocket и печатает
приходящие уведомления. Демонстрационная часть ЛР №4.

Переменные окружения — см. `.env.template`.

Общее описание монорепо и трейдоффы подхода — в корневом [`README.md`](../README.md).
